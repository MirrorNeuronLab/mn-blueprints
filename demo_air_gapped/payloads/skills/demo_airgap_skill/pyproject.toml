[project]
name = "mn-demo-airgap-skill"
version = "1.3.47"
description = "Blueprint-owned note analysis helpers for the air-gapped demo."
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
