# General Python Defined Basic

This blueprint is authored as pure Python with `mn_sdk` decorators. `generate_bundle.py`
compiles the Python workflow into a normal MirrorNeuron bundle.

```bash
python3 generate_bundle.py --quick-test
mn run .
```
