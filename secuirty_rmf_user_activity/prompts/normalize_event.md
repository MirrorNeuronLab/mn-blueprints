# Normalize Event Prompt

Convert raw user activity into the supported v1 event schema. Preserve source event IDs, timestamps, user/session identifiers, source IP, geo fields, device fields, resource fields, and unknown source fields inside metadata. Do not invent fields that were not present.

